torch>=2.1.0
# Qwen3.5 text-only loading and correct text-checkpoint saving require the
# recent Qwen3.5 implementation. The model cards recommend the latest build.
# transformers>=5.13 uses the new-style response_template parser (handles the
# template-emitted <think> prefix); 5.5.4–5.12 fall back to the legacy
# response_schema parser, which also works with the nothink template.
transformers>=5.5.4
datasets>=2.20.0
peft>=0.13.0
# Manager GRPO uses GRPOTrainer(tools=/environment_factory=) plus
# GRPOConfig(max_tool_calling_iterations=, chat_template_kwargs=), which
# require trl>=1.7.1. jmespath is TRL's tool-call response-parsing dependency.
trl>=1.7.1
jmespath>=1.0.0
accelerate>=0.34.0
pydantic>=2.0.0
anthropic>=0.40.0
openai>=1.50.0
pyyaml>=6.0
numpy>=1.24.0
tqdm>=4.66.0
wandb>=0.18.0
